<Directory /var/www/html>
    Options FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>

# 禁止直接访问源码与数据目录
<DirectoryMatch "^/var/www/html/(src|data|docker)">
    Require all denied
</DirectoryMatch>

# 长轮询接口不宜被 Apache 过早超时掐断（core 指令，无需 mod_proxy）
Timeout 300
